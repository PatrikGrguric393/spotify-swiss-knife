@using System.Text.Json
@model List<spotify_swiss_knife.Models.Album>

@{
    ViewData["Title"] = "Library - Albums";
}

<section class="library-page">
    <h1>Library: Albums</h1>
    <div class="library-filters">
        <div class="library-actions">
            <div class="search-box">
                <label for="albumSearch" class="visually-hidden">Search</label>
                <input id="albumSearch" name="q" type="search" class="search-input" placeholder="Search albums..." aria-label="Search albums" />
                <span class="search-status" aria-live="polite"></span>
            </div>
        </div>

        <div class="date-range-filter">
            <div class="date-range-filter__field">
                <label for="albumDateFrom" class="date-range-filter__label">From</label>
                <input id="albumDateFrom" type="date" class="search-input date-range-filter__input" />
            </div>
            <div class="date-range-filter__field">
                <label for="albumDateTo" class="date-range-filter__label">To</label>
                <input id="albumDateTo" type="date" class="search-input date-range-filter__input" />
            </div>
            <button type="button" id="albumDateClear" class="date-range-filter__clear">Clear</button>
        </div>
    </div>

    <p>All albums from the mock repository.</p>

    <div class="table-actions">
        <a class="btn btn-primary" href="@Url.Action("CreateAlbum", "Library")">Create Album</a>
    </div>

    <div class="table-wrap">
        <table class="entity-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Artists</th>
                    <th>Release Date</th>
                    <th>Details</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var album in Model)
            {
                var artists = string.Join(", ", album.Artists.Select(artist => artist.Name));
                var detailsId = $"album-details-{album.Id}";
                var detailPayload = new
                {
                    album.Name,
                    Artists = artists,
                    Type = album.AlbumType,
                    ReleaseDate = album.ReleaseDate,
                    TotalTracks = album.TotalTracks,
                    album.Label,
                    album.Popularity
                };

                <tr>
                    <td data-label="Name">@album.Name</td>
                    <td data-label="Artists">@artists</td>
                    <td data-label="Release Date">@album.ReleaseDate</td>
                    <td data-label="Details" class="cell-actions">
                        <button type="button" class="entity-detail-trigger btn" data-details-id="@detailsId" data-entity-type="Album">
                            View
                        </button>
                        <script type="application/json" id="@detailsId">@Html.Raw(JsonSerializer.Serialize(detailPayload))</script>
                    </td>
                    <td data-label="Actions" class="cell-actions">
                        <a class="btn" href="@Url.Action("EditAlbum", "Library", new { id = album.Id })">Edit</a>
                        <a class="btn btn-danger" href="@Url.Action("DeleteAlbum", "Library", new { id = album.Id })">Delete</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</section>

<div class="entity-detail-backdrop" id="entityDetailBackdrop" hidden>
    <section class="entity-detail-modal" role="dialog" aria-modal="true" aria-labelledby="entityDetailTitle">
        <div class="entity-detail-header">
            <h2 id="entityDetailTitle">Details</h2>
            <button type="button" class="entity-detail-close" id="entityDetailClose" aria-label="Close details">Close</button>
        </div>
        <div class="entity-detail-body">
            <div class="entity-detail-content" id="entityDetailContent"></div>
        </div>
    </section>
</div>

@section Scripts {
    <script src="~/js/library-albums.js" asp-append-version="true"></script>
    <script src="~/js/library-details.js" asp-append-version="true"></script>
}