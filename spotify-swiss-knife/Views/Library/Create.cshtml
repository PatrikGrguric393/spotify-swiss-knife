@using spotify_swiss_knife.Models.FormModels;

@model ArtistCreateModel

@{
    ViewData["Title"] = "Create Artist";
}

<div class="form-container">
    <h2>Create New Artist</h2>

    <form method="post" id="createArtistForm" data-validate="true">
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label for="Name" class="required">Artist Name</label>
            <input
                type="text"
                id="Name"
                name="Name"
                class="form-control"
                value="@Model?.Name"
                required
                maxlength="300"
                data-val-length-max="300"
                data-val-length-min="1"
                placeholder="Enter artist name"
                aria-label="Artist Name"
                aria-describedby="error-Name"
            />
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Create Artist</button>
            <a href="@Url.Action("Artists", "Library")" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
