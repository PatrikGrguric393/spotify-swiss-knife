name: Build and push SSK's container image
run-name: ${{ gitea.actor }} is building and pushing SSK's container image
on: create

jobs:
  build-and-push-image:
    runs-on: builder-debian-12
    container:
      image: ghcr.io/catthehacker/ubuntu:act-latest
    env:
      REGISTRY_URL: ${{ vars.GIT_DOMAIN }}
      IMAGE: ${{ vars.GIT_REGISTRY_USER }}/${{ vars.IMAGE_NAME }}
    steps:
      - name: Log in to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.GIT_DOMAIN }}
          username: ${{ vars.GIT_REGISTRY_USER }}
          password: ${{ secrets.CONTAINER_REGISTRY_TOKEN }}
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build image and push it to the registry
        run: |
          docker buildx create --name container --driver=docker-container
          docker buildx build --builder container --platform linux/amd64,linux/arm64 -t ${REGISTRY_URL}/${GIT_USER}/${IMAGE}:${{ gitea.ref_name }} --push .          
      - name: Restart deployment rollout in Kubernetes to apply new image version
        uses: actions-hub/kubectl@master
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        with:
          args: -n ssk rollout restart deployment spotify-swiss-knife

